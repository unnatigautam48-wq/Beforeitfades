# Visual verification

The desktop preview at 1280×720 shows the intended dark legal-operations workspace: persistent sidebar, premium typography, amber time-simulation control, radar with five evidence signals, and a selected source-linked CCTV detail panel. The hierarchy reads clearly in the first viewport.

The mobile preview at 390×844 collapses the sidebar into a menu button, keeps the case header and action buttons usable, stacks the time-simulation controls, and preserves the radar as the primary visual surface. The headline wraps cleanly without horizontal overflow.

Validation status: `pnpm check` passed, `pnpm build` passed, desktop and mobile screenshots captured successfully.
